name: mapbox4dart
description: Pure Dart codec and object model for Mapbox GL Style JSON files.
version: 0.1.0
repository: https://github.com/pt9912/mapstyler
resolution: workspace

environment:
  sdk: '>=3.7.0 <4.0.0'

dev_dependencies:
  test: ^1.25.0
  lints: ^6.1.0
