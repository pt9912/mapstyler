# Changelog

## 0.1.1

- Updated the `flutter_map` dependency constraint to 8.x.

## 0.1.0

- initial `flutter_mapstyler` package setup
- added `StyledFeature` and `StyledFeatureCollection`
- added expression and filter evaluators for `mapstyler_style`
- added rendering for fill, line, mark, icon, text, and raster symbolizers
- added optional per-feature tap and long-press callbacks
- added geometry helpers and package test coverage
